package org.optioignon.mobile

import android.app.Activity
import android.os.Bundle

/**
 * SKELETON entry point. The real UI is host-side work; this is a placeholder so
 * the module builds. The sync and inference clients live in the `sync` package
 * and are driven from there, not from this activity yet.
 */
class MainActivity : Activity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // No UI yet (skeleton).
    }
}
